require "spec_helper"

describe Receipt::Service do
  it "has a version number" do
    expect(Receipt::Service::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
